# Elfred v27 · 新手初始化与完整 Agent 朋友圈

在 v26 基础上升级的移动端交互原型。注册、新手资料、Agent 初始化和主页使用白色／浅灰、24px 卡片、胶囊按钮和少量蓝绿强调色。

## 如何体验

- 首次打开：注册／登录演示 → 演示验证码 → 选择快速或完整初始化。
- 不注册也可体验。注册页的验证码固定为 `123456`，页面有明确提示；不会发送短信或邮件。
- 个人初始化：昵称、身份、城市、近况、最多三个目标、优先帮助方向、表达偏好、边界、可选资料、个人卡片确认。
- Agent 初始化：探索、参谋、创作、连接、执行；可逐个设定名称、关注方向和表达方式，也可全部默认或稍后设置。
- 选择 30／80／150 条每日内容预算和提醒偏好，确认权限边界后进入主页。
- 在“个人”页顶部，或主页“小队 → 管理”，可重新进入初始化。信息修改会保留。

## 主页与 Agent 空间

- 今日简报、实际示例动态数、待确认任务数、进行中任务数。
- 五个 Agent 独立入口；每个有主页、对话、任务、记忆、养成五个区域。
- 推荐／最新／待决定／进行中筛选；关键词搜索、按 Agent 筛选、收藏筛选。
- 帖子详情、跨 Agent 示例评论、用户评论、喜欢、收藏、不感兴趣与理由。
- 用户评论先成为待确认记忆，可确认或仅用于当前讨论；记忆可编辑、锁定和删除。
- 帖子转任务、任务草稿、确认开始、模拟完成、重新打开；支持一次性／周期性／条件触发类型展示。
- L1—L15 成长路径与 L6 条件说明。演示数据不会自动解锁“真实懂你”认证，也不会改变权限。
- 下拉或点击顶部入口打开 Skill／Mini App 模板；任务入口保留通向 v26 原日程页的路径。
- 保留 v26 的知识库、社区、消息、个人档案及原有详情结构；个人页称呼和城市与新手资料联动。

## 原型边界

这是可交互前端原型，不是已接入真实 AI 的生产服务。页面内也会显示相应说明：

- 没有真实账号注册、登录验证、SMS、邮件或 OAuth。
- 没有连接真实 LLM、RSS、日历、邮箱、知识库或外部工具。
- 示例帖子不是实时新闻；Agent 回复是用于演示的模板内容。
- 30／80／150 是内容预算配置，当前不后台生成或推送对应数量的内容。
- 选择文件只保存文件名，不上传、不解析文件内容。文本介绍保存在当前浏览器。
- 个人资料、设定、评论、收藏、记忆和任务保存在浏览器的本机演示数据中；不会跨设备同步。
- 模拟任务状态变更不会执行外部操作。发送、付款、删除与公开发布的权限不会随等级自动解锁。

## 本地启动

需要 Node.js 22.13 或更新版本。

```bash
npm ci
npm run dev
```

在终端显示的本地网址中打开。生产构建：`npm run build`。类型检查：`npm run typecheck`。基础产品检查：`npm test`。

## 主要代码

- `app/onboarding.tsx`：注册、个人资料与 Agent 初始化。
- `app/agent-home.tsx`：主页、动态、任务台、Agent 独立空间。
- `app/product-state.tsx`：本机演示状态、五个 Agent、内容样例、成长条件。
- `app/product.css`：v27 视觉系统与响应式适配。
- `app/page.tsx`：v26 原页面与新版流程整合。

版本依赖说明：为使用已验证的本地依赖缓存，图标库锁定为 Lucide 1.31.0；兼容的传递依赖 Zod 锁定为 3.25.76。原项目其余依赖保持不变。
