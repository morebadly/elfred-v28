
# @ 调用弹层 · 2026-09-22

final result: passed

范围：仅本次用户要求的 @ 弹层及输入框状态。参考为 d390b19c2e8d3436a82c8e40cb8f9883.jpg；实现为 http://127.0.0.1:3000/v28 的真人聊天。现有设备框、账号、真实聊天内容与参考不同，不纳入本次复刻。

参考和最终浏览器截图已在同一次比较输出中打开。白色圆角容器位于输入框上方，含“调用”、黑色图标、“我的 Elfred”、Personal Agent 副标题、右箭头、分隔线及继续输入提示。输入框保留 @，有波形语音按钮和黑色发送/选择按钮。

第一次比较发现输入框出现多余黑色焦点矩形、语音图标为麦克风，均已修复；最终截图无阻挡输入或内容溢出的 P0/P1/P2。P3：图标沿用 Lucide Orbit，与参考的单行星线形略有区别。

已验证：手动 @ 才打开；继续输入“报名”匹配已有知识；删除 @ / Escape 关闭；ArrowDown 聚焦卡片后 Escape 返回输入框；点击我的 Elfred 才打开私密辅助；退出后原消息草稿保留；本次 QA 草稿已清空。没有发送真人消息或发起模型请求。

代码审查修复：异步保存完成回调采用会话/选择代次保护；切换会话重置忙碌状态；输入框在可用状态渲染后恢复焦点。

构建及 TypeScript 通过；5 项 @ / 初始化相关集成测试通过；最终浏览器控制台无 error/warn。检索范围为应用内可见文件、知识、资源与自定义工具；外部插件和操作系统文件夹搜索未接入。
