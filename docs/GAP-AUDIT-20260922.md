> 本文保留当时的阶段检查记录。后续补全及当前验收见 [首页与信息页补全记录](HOME-MESSAGES-COMPLETION-20260922.md)。

# 产品代码缺口审计（2026-09-22）

> 本报告记录本轮补写前的缺口。用户随后将重点收敛为首页（含社区）与信息页；该范围内的等级、简报、社区和消息修复及验证见 [HOME-COMMUNITY-MESSAGES-20260922.md](./HOME-COMMUNITY-MESSAGES-20260922.md)。下表保留初始审计事实，不应将已补项继续当作当前未实现项。

结论：当前是接入本地持久化服务的部分产品实现，不能称为“16 模块全部完成”。缺口不止等级，也不只是模型密钥未配置。此前接线记录把“已有入口、能保存、基本路径可运行”表述得过于接近“完整交付”，需要纠正。

## 核对范围与判定方式

- 需求基线：`D:/软工任务/Elfred-全产品需求与技术体系-20260921/01-模块需求` 中 M01—M16，以及共用契约、技术架构与决策清单；等级专项方案作为历史设计依据，冲突按新需求处理。
- 实际入口：`/`、`/v28` → `app/v28/core/app-shell.tsx` → 原版页面及 RuntimeProvider → `server/elfred`。`/prototype` 的演示内容、未路由的 `features/live` 页面不计入已交付功能。
- 检查方式：需求对照、前后端调用与命令处理器检查、隔离内存数据库探针。没有把接口名字不同直接判为缺失，也没有以测试数量推算完成百分比。
- 下文“部分实现”表示已有代码可复用，但仍缺业务行为；“未实现”表示当前实际路径未找到相应实现；“待配置验证”仅用于适配代码已存在的情况。需求标记 C 为确定要求、D 为设计细化；未定参数单独列出，不伪称已定。

## 逐模块缺口

| 模块 | 已有实现 | 仍需补写或接通的代码 |
|---|---|---|
| M01 初始化与首次价值 | 本地密码账号、会话、资料、意图保存 | 引导步骤和回答持久化/断点恢复；模式与同意记录；由初始意图产生真实任务或成果；首次价值引用与完成条件。现在可以没有任何任务或成果就完成初始化。 |
| M02 首页与三时段卡 | 任务列表、简报对象、确认和反思记录、任务播放器 | 早间重点/中间调整/晚间结果的真实写回动作；按用户时区和作息生成当日简报；服务端统一偏好与跨设备恢复；简报历史和冻结版本；最近/常用工具与内容进入收件箱的完整承接。当前确认主要记录已读。 |
| M03 真人关系与消息 | 好友申请/响应、文本私聊群聊、未读、结构化提及、部分成员撤权 | 消息附件/语音、历史分页与实时订阅接线；群成员增补、群主交接等管理能力；屏蔽后的完整管理入口。前端主要轮询快照，不能据此称完整实时消息系统。 |
| M04 私聊与本人辅助 | 选择消息、授权运行文本任务、私人草稿保存/填入 | 原设计的多能力选择与检索支撑；回复、笔记、任务、日程、简报等结构化结果及确认承接；运行完成后辅助对象状态同步；日程适配缺失时的草稿/导出。现在核心是通用文本生成。 |
| M05 群聊与共同状态 | 真人提及、本人辅助、承诺提出/接受/拒绝 | 共同目标/结论/分歧等共享记录；承诺来源、期限、修订、撤回、重新确认；结论维护人与个人承诺的不同操作权限；相关任务、日程、简报同步。 |
| M06 社区与 AI 共创 | 项目招募、申请审查、文本副本、贡献审核、静态文本发布/回滚、再创作 | 普通图文帖创作；多个任务位、期限/容量、停止招募、退出及项目暂停/终止；成员 Agent 任务与依赖协同；可运行软件产物的版本/合并、沙箱预览、构建验收和发布；举报处理闭环。文本版本链不能代表完整软件共创。 |
| M07 搜索与文档解析 | 权限过滤后的关键词包含匹配；UTF-8 txt/md/csv/json 导入；片段行号 | 自然语言条件解析、语义召回/混合排序、约束判断；人物/Skill 检索；PDF/Word/PPT、图片 OCR、音频解析；解析任务、分块与失败重试；点击后定位原消息/原文位置；失败与空结果区分。Jev 没有接入。 |
| M08 知识与长期资产 | 知识创建/归档、资源和资料记录、任务验收保存知识 | 知识编辑、文档修订和版本差异、标签/适用边界、证据异议；能力投影；Agent 阶段/等级展示、成长记录、勋章及真实证据关联；Skill 版本、测试、启停/归档、使用记录/熟练度。工具创建入口目前落到资产编辑器。 |
| M09 我的与账户 | 资料和图片、部分隐私控制、简单公开姓名/简介、导出和退出 | 可选择的能力/成果公开投影与访客可见范围；真实二维码/海报；修改/找回密码、会话设备管理、关联账号；删除申请的执行和进度。删除当前只登记为 `pending_policy`。 |
| M10 PersonAgent 与五系统 | 五系统配置、17 个职责名称、任务所属系统 | 按目标选择主责系统与专业角色；有输入/输出/工具/评估约束的角色定义与按需实例；系统间最小上下文请求/同意/拒绝/过期；系统与角色分别计算的对齐状态。17 个名称不等于 17 个已运行的专业能力，也不要求拆成 17 个服务。 |
| M11 调度与执行恢复 | 草稿/确认、单次延迟启动、暂停/取消、租约/检查点、回执、人工验收、反馈重跑 | 根据目标生成依赖计划并综合多步结果；按验收标准核查结果；有界定向修复/重规划闭环；目标修订及授权版本联动；周期/条件 Schedule、触发唯一标识、错过执行/时区策略和计划暂停。已有 DAG 校验器，但默认计划生成基本仍是一个工具步骤。 |
| M12 记忆上下文与对齐 | 手工记忆候选、确认/纠正/拒绝/删除、派生失效、单条记忆情境证据 | 自动候选提取与重复拒绝抑制；矛盾处理、到期治理；按任务目的构造最小上下文包；任务/系统范围绑定；把单条证据汇总成系统/Agent 能力视图。现有记忆分类映射与原页筛选项不一致。 |
| M13 反思评测与 Skill 改进 | 简化轨迹、Prompt 候选、两项留出运行、人工比较、方法发布/回滚、启动版本固定 | 完整轨迹准入与二次使用同意；失败归因；Skill/路由/工作流类型化补丁；冻结评测集和逐项安全/质量/成本/延迟门槛；维护者发布权限与父版本；影子运行、有限灰度、监测和退化回退。现有通用方法面板不等于需求中的完整 RSI。 |
| M14 Agent 朋友圈与收件箱 | 任务验收产生去重动态、收藏/隐藏、基础系统/时间/类型筛选、收件箱对象 | 发现/理解对齐/Moment 等不同价值事件触发；其他系统有依据的补充评论；主题关注/屏蔽、推荐排序、隐藏撤销入口；内容到待办意图的完整拖入承接。当前真实动态基本只有验收成果一种来源，类型映射也固定。 |
| M15 连接与外部工具 | 本地搜索/文档读取、资源记录、文本模型适配器、模型范围授权/撤销 | 连接需求解析→证据匹配候选→联系草稿→本人确认后的执行；外部工具适配与回执对账；连接凭据管理/刷新；连接授权与具体发送/发布操作授权分开。当前产品工具白名单仅本地检索、文档读取、文本生成。 |
| M16 权限费用通知与运营 | 对象权限、任务批准、预算预留、人工用量对账、通知记录、幂等/CAS、备份恢复 | 通用资源/动作范围授权；按真实用量与价格版本核算、项目付款主体/转移确认；安静时段和通知偏好参与投递决策、渠道去重/重试；通知定位原对象；生产保留/删除策略执行、监控及运营控制。保存开关不等于实现通知投递策略。 |

上述表中包含确定需求及设计细化。第三方供应商、游戏化分数、运营阈值等仍应按决策清单落实，不能任意填数字后宣称完成。

## 最关键的代码证据

1. **等级设计未完成，原设计也被简化替换。** `app/v28/legacy/legacy-ui.tsx:3015` 的真实运行分支固定显示“有待验证”，并说明不生成百分比与等级；原等级/成长/勋章展示留在演示分支。`server/elfred/knowledge.mjs:60` 只有单条记忆证据分级，没有等级或勋章投影。M08 明确完整游戏化算法未定，但这不能作为删掉既有设计、换成固定占位文案并宣称交付的理由。应保留原布局，以证据驱动已定的四阶段对齐；数值、奖章条件另列待定。
2. **初始化没有首次价值闭环。** `server/elfred/service.mjs:52` 的保存/完成处理器只写有限字段；`app/v28/core/app-shell.tsx:239` 直接完成并回首页。步骤、回答、首次结果引用没有形成持久业务链。
3. **简报确认没有执行调整。** `server/elfred/service.mjs:127` 仅更新 `acknowledged` 和备注，没有执行确认动作、更新任务或写入理解。首页展示不能代替这个行为。
4. **专业角色和复杂执行尚未落地。** `server/elfred/catalog.mjs:1` 注册职责名称；`server/elfred/runtime.mjs:50` 生成固定工具计划。`server/elfred/policy.mjs:49` 主要核查回执，文本生成交人工审查，本地检索/读取有非空结果便可通过，未按完整目标标准验证。
5. **工具/Skill 接线不完整。** `app/v28/core/app-shell.tsx:526` 的“创建工具”入口转到资产编辑；`server/elfred/service.mjs:141` 保存简单方法草稿；`app/v28/core/runtime-panels.tsx:89` 复用时创建文本任务，没有将 Skill ID/版本和真实使用记录贯穿执行。
6. **搜索、解析仍是基础实现。** `server/elfred/knowledge.mjs:15` 按空白切词并使用 `includes`；`:35` 限定四种文本格式。`server/elfred/providers.mjs:39` 虽定义规则 Judge 类，但没有 Jev 适配及完整判定接线。搜索显示语义状态未知是诚实降级，不能算语义检索已实现。
7. **原记忆分类有实际接线错误。** `app/v28/core/runtime-context.tsx:51` 将真实记忆统一映射成“偏好”，而 `legacy-ui.tsx:5431` 的可选分类是基础/社交/近况/习惯，`:5538` 又按精确相等过滤；因此分类筛选无法正确展示真实条目。
8. **共创目前是文本版本协作。** `server/elfred/community.mjs:42` 的副本与 `:78` 的发布没有软件项目运行/构建链路；普通帖子创作也没有对应完整命令。应保留已写的权限、版本和审核代码，补足产物与协作执行层。
9. **通知偏好只保存，费用尚非真实结算。** `server/elfred/service.mjs:71` 写 `notifications/quiet`，未找到通知投递调度读取它们；`server/elfred/runtime.mjs:163` 使用固定的模型预算单位，而非最终账单价格核算。通知记录继续存在本身符合“应用内待办持续可查”，不能仅因免打扰时生成了未读记录就认定违规；真正缺的是投递策略层。
10. **RSI 是简化人工流程。** `server/elfred/improvement.mjs:19` 固定两项留出任务；`:49` 发布后直接成为用户后续运行的活跃方法。没有完整影子/灰度、独立发布权限和退化监控控制器。
11. **生产入口尚未接通业务服务。** `worker/index.ts:1` 仍是模板 Worker，`db/schema.ts:1` 为空；当前实际业务后端是单独启动的 Node + SQLite。本地启动通过不代表原部署链已支持这些业务 API。

## 隔离执行证据

复现脚本：`.elfred-data/gap-audit-20260922.mjs`，使用 `Store(':memory:')` 和独立审计用户，不访问真实模型，不改当前用户数据库。执行命令：`node .elfred-data/gap-audit-20260922.mjs`。

| 检查 | 观察结果 | 能支持的结论 |
|---|---|---|
| 保存步骤后完成初始化 | `completed`，步骤未保存，首次价值引用空，任务/收件箱均为 0 | 首次价值和引导恢复链没有实现 |
| 确认简报 | 简报变为 `acknowledged`，任务未变，确认动作列表为空 | 当前确认不落实计划调整 |
| 为“检索、核对、生成结论”启动 compose 任务 | 计划只有 `text.compose`，无依赖 | 当前运行不会按复杂目标自动拆解该流程 |
| 关闭通知并开启免打扰后发送提及 | 仍生成未读站内记录 | 只能证明站内记录存在；结合静态检查确认没有渠道投递策略，不能把站内留存本身当错误 |
| 同一文档关键词与句子搜索 | 关键词命中，整句未命中 | 配合搜索源码说明未做自然语言条件解析；该探针不是语义质量评测集 |
| 知识更新、周期计划、上下文请求、普通帖候选命令 | 返回 `UNKNOWN_COMMAND` | 与处理器清单核对后确认相应业务未形成；不以命令拼写差异单独判缺失 |

## 外部依赖与尚未确定的产品规则

| 项目 | 准确状态 |
|---|---|
| 文本模型调用 | Chat Completions 兼容适配已写；当前环境待配置，真实调用、质量和账单未验证。填密钥不会补齐规划、搜索、角色调度等缺口。 |
| Jev | 没有真实适配；协议、认证、计费等还需按最终决策落实。不能仅写成“待填密钥”。 |
| 短信、Apple/微信登录、语音、日程等 | 当前不具备完整对应适配与业务流程。具体首期供应商/范围待确定，同时确实有待写代码。 |
| 等级/勋章 | 四阶段对齐、Agent 成熟度与 Skill 熟练/活跃状态的区分应实现；完整游戏化评分、级数、阈值未定，不能擅自用调用次数升级或解锁权限。 |
| 支付、生产删除和运营阈值 | 有规则/服务决策依赖。真实支付不能无依据列为当前确定必交功能；删除执行、真实费用对账与运维支撑也不能因参数未定就写成已完成。 |
| 生产架构 | PostgreSQL/搜索索引、对象存储、持久 Worker 与部署适配未交付；具体实现可按已批准架构调整，验收应看功能与可靠性，不只看技术名词。 |

## 补写顺序与验收边界

1. 先恢复并核对原设计的实际运行分支：等级/成长/勋章、知识分类、创建工具、能力视图；建立字段和真实状态映射，避免再用通用表单替代原功能。
2. 补核心业务：首次价值、简报确认写回、角色选择/上下文、计划生成/结果验证、周期调度、Skill 版本和复用。
3. 补跨模块链路：多格式解析与搜索定位、聊天结构化结果、群共同状态、可运行共创产物、Feed 多事件、通知策略、真实费用对账。
4. 最后按选定供应商接入并验证外部服务，补生产部署、RSI 灰度与运维；逐条对照需求 T-Mxx 验收，不以“入口可点”替代业务通过。

此前 24 项产品测试、33 项运行时测试，以及类型检查/本地构建通过，只能证明它们覆盖到的基础路径。它们未覆盖全部 16 模块、原设计完整性、真实外部供应商和生产故障场景，不能作为“全部接通”的证据。本次为代码与需求审计，并未完成上述缺口的开发。
