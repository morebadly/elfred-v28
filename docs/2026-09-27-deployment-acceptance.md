# 旧 Elfred 服务器部署验收

日期：2026-09-27。线上入口：http://111.230.26.68/v28。

## 部署结果

- 旧 Elfred 已被当前 V28 替换，根路径跳转到 /v28；原应用和 Nginx 配置完整备份在 `/www/backups/elfred-before-v28-20260927`。
- 运行目录 `/www/wwwroot/elfred-mobile/current` 指向 `releases/20260927-meeting-v28`；PM2 仅更新 `elfred-mobile`，其他服务器应用保持正常。
- Linux Node 22.13.1 生产构建及 174 项后端测试全部通过，TypeScript 测试启用原生类型剥离；Office 使用 python3，PDF 解析依赖已安装。
- 模型 Key 仅在服务端私有目录，文件权限 600；数据目录权限 700。没有把本地账号数据、千级假数据或密钥上传 GitHub。`.env.local` 公网请求返回 404。
- Nginx 保留公开 Host 与 Origin，关闭推送缓冲；服务保存登录和任务状态，PM2 已保存重启配置。

## 实际功能验证

| 检查 | 结果 |
| --- | --- |
| 公网健康检查及 /v28 | 正常，HTTP 200 |
| 公网手机界面与登录恢复 | 390×844 实际浏览器打开并截图，无运行异常 |
| 已配置模型 | gemini-3.5-flash-lite；服务器实际调用通过 |
| 子 Agent 多轮 | 同一会话两轮回复，历史保留；普通对话未自动变为可见草稿 |
| 群聊协作 | 两个隔离测试账号同意后，以 AI 身份给出公开日程建议；未代签个人承诺，撤权后保存状态 |
| RSS 自动获取 | 初始化确认后后台自动读取真实 RSS，匹配 3 条公开资料；真人社区没有 Agent 帖子；验收账号的自动关注已停止 |
| 资讯来源可用性 | 少数派、爱范儿在服务器实际读取通过；36氪返回非 RSS，已替换。Google News 不可达时采用备用来源 |
| 输入与入口 | 手机实际检查 Shift+Enter、输入 @、收件箱、蜂群分工顺序、共创预览点击 |

## 证据位置

本地 `2026-09-26会议交付/手机验收/` 包含六段 MP4、手机截图、空态效果与逐项 JSON。三份短文档已更新。线上模型和群聊验收使用独立测试账号，不向真实用户发送消息。

之前服务器连接失败源于本机代理隧道的出站路径。指定 Wi-Fi 出站后 SSH 与公网页面正常，不修改系统代理设置。若此电脑的默认代理仍拦截该 IP，请将这个服务器地址设为直连，或使用手机正常网络访问。

## 仍需外部条件

Jev 未提供真实服务接口或配置；邮件、支付、网页代操作尚未选定服务；非开发人员初始化试用仍需要真人反馈。这些不作为已接入或已完成的验收项。工作群同步材料已备好，未代本人向工作群发送。
