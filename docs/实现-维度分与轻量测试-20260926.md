# 实现：用户五维分（人级）+ 新用户「轻量测试」

> 这份文档记两轮：
> **第一轮**把"卡的分（skill 级）"和"人的五维分（人级）"拆开；
> **第二轮**（同日，按用户口径）改成 **测试给起点 · 起点也进综合分 · 题库换 IPIP 混合**。
> 现在这份写的是**当前真实状态**，自测全绿（清单见最后一节）。

## 一、主体拆开：卡的分 ≠ 人的分

### 之前错在哪
`insight.axes_now()` 是"该维度下所有卡的分数按成果数加权平均" —— 直接把 **skill 的分**
当成了 **人的能力**。多建几张卡就涨、随手的小事和难事一个待遇、样本少也不打折、久了也不会退。

### 现在怎么算（`app/services/dimensions.py`）
每维维护一个后验 `(θ, σ)`：**起点 = 那份轻量测试**，**观测 = 每条成果自己的表现分**，
闭式高斯更新：

```
precision = 1/σ0² + Σ w_i/τ²
θ         = (θ0/σ0² + Σ w_i·y_i/τ²) / precision
σ         = sqrt(1/precision)
lower     = θ − 1.5σ        （保守值；与卡的分同一坐标系）
```

| 参数 | 值 | 含义 |
|---|---|---|
| `τ` | 12 | 单条证据的观测噪声。**调小 = 做一件事就跳一大截**（用户明确说过"随手做一件事就 64 分不对"），所以给宽一点 |
| `σ0` | 10（+ 最多 4） | 起点的标准差；作答越不一致（正/反向项打架）给得越宽 |
| 半衰期 | 30 天 | 证据按 `0.5^(龄期/30)` 衰减 → **久了会退** |
| `MIN_EVIDENCE` | 3 | 够 3 条才算"这一维站住了"；**没做过测试**的账号要靠这 3 条才给数字 |
| 权重 `w` | 外部可核对 1.0 / 本人验收 0.35 / 未验收或纯代做 0.2；带指正记录 ×1.2 | 来源越硬，说话越算数 |
| 难度校正 | 小 0.9 / 中 1.0 / 大 1.15 | `y' = 50 + (y−50)/b` |
| `START_BAND` | 22 – 52 | 测试起点映射到的区间，**整体压在卡片基线 60 之下** |

为了让"每条成果自己的表现"有据可依，给 `evidence` 表补了两列（`app/core/migrate.py` 幂等 ALTER）：
`outcome`（这一次的表现分）、`difficulty`（这件事多大）。三条写入路径都落：
手动加成果（`/evidence`）、本地真跑 skill（`ingest`）、桥接他那边验收（`bridge`）。

**顺手修掉的一个真 bug**：`POST /evidence` 原来把**每一条**成果都写成
`weight_label="计为 1 次外部验证"` —— 人的维度分是按这个标签读权重的，等于
"自己点一下"被当成"外部证实"，随手一件事就把分推上去。现在统一成 `权重 {w}`（和 bridge/ingest 一致），
而 `_weight_of()` 读不出数字的旧标签按 **0.8（本人验收）** 兜底，**绝不默认成外部级**。

## 二、口径修正（用户 2026-09-26 定，别再改回去）

1. **新用户不画全 0 的图**：没做过测试就还是空态（只有「开始测试」），不是一张全是 0 / 全是未知的五维图。
2. **测完立刻有一张起始的五维图**（22–52，偏低）**和一个综合分**。
3. **起点计入综合分**：不再有"自评不计入综合分"这套东西 —— 起点是坐标系的低端，不是"不算分"。
4. **雷达只有一种画法**：不另画一条淡色虚线、不把某几维标灰。说明只留一句
   「起点来自那次测试 / X 已经按真实成果动过」，**没有别的角标**（原来右边挂过
   「做完事会变，久了也会回落」，用户说多余，删了）。
5. **它是动态模型**：做出成果往上走，**被指正、判错、长期不用会回落**（和理解度同一种性质）。

## 三、题库（IPIP 混合方案，22 题）

| 维度 | 题目来源 | 题数 |
|---|---|---|
| 洞察 | IPIP **开放性**（`o1–o4`） | 4 |
| 交付 | IPIP **尽责性**（`c1–c4`） | 4 |
| 表达 | IPIP **外向性**（`e1–e4`） | 4 |
| 链接 | IPIP **宜人性**（`a1–a4`） | 4 |
| 判断 | **自研情景题**（`j1–j6`，3 档选项） | 6 |

- **许可**：IPIP（International Personality Item Pool）是**公有领域**，可自由翻译、改写、商用；
  我们照它的**原题方向键**走（每题 `key: 1 / -1`，正反向各半：13 正 / 9 反）。
- **为什么不要 IPIP 的情绪稳定性那 4 题**：我们量的是"做事的能力"，不是人格诊断；
  拿"我常有情绪波动"去定「判断」这一维，用户看不懂分数是怎么来的。
- **计分**：选项顺序 = 分值顺序（低 → 高）；反向题翻过来；**每题先归一到 0..1**
  再按维度取平均，最后映射到 `START_BAND`（5 档量表题和 3 档情景题不会因为档数不同被拉高/压低）。
- **作答一致性（guard）**：正/反向项的均值差 —— 一路点"同意"的人在两侧会自相矛盾，
  guard 接近 1 → 起点更不确定（σ 更宽）。**不额外占题位、也不单独算分**（上一版的"校准题"删了：
  用户明确问过"自评不进入综合分这种东西存在的意义是什么"）。
- 题目顺序交错排（同一维的题不挨着），避免答题人被上一题带着走。

## 四、实测（真跑，不是口算）

算法自测 `tools/dimension_selftest.py`（20 项全绿）里的关键几行：

```
每维都答成最弱 → 五维全 22.0 ；都选中间 → 全 37.0 ；每维都答成最强 → 全 52.0
一路点同意     → guard=1.0（作答不可信）；一贯作答 → guard=0.0
测完           → 交付 37.0 · 综合 35 · source=baseline
1 条外部成果   → 交付 62.5（growing）      ← 会动，但没一步到位
3 条外部成果   → 交付 75.2（evidence）     ← 够 3 条才"站住"
其中一条判错   → 交付 60.8（−14.4）        ← 真的会掉
90 天没有新证据 → 交付 50.9（向起点 37 收缩）← 真的会回落
```

界面自测 `tools/page2-checks/questionnaire-check.mjs`（24 项全绿）里的关键几行：

```
新账号空态有「开始测试」、没有「自评」字样
答完 22 题 → 完成页「你的起点」：判断/洞察/交付/表达/链接 = 37 · 37 · 37 · 37 · 37
回第二页   → 雷达画出来、综合能力 37/100、说明「起点来自那次测试」
1 条本人验收（0.8）→ 交付 37 → 47.4（还在 growing）
再加 2 条外部（1.0）→ 69.9（evidence）
判错 1 条          → 54.0（掉下来）
```

## 五、接口口径

- `GET /page2/questionnaire`：题目 + 选项（**不含任何内部字段**：没有 `key`、没有 `guard`）。
- `POST /page2/questionnaire`：存一份 `dimension_baselines`（原样保存作答，可回溯重算；
  `active` 保证一人一份有效起点，重测覆盖并留历史）→ 返回起点值 + `guard` + 最新估计。
- `GET /insight/abilities`：每维 `value / lower / source / samples / missing / uncertainty`，另加
  `baseline / started / verifiedDimensions / minEvidence`。
  `source` 四档：`baseline`（只有起点）· `growing`（起点 + 1~2 条成果）· `evidence`（够 3 条）·
  `insufficient`（没测试也没攒够 → **不给数字**）。
  **`composite` = 有值的维的平均（起点也算）**，仍要求 ≥3 维有值。
- `GET /dimensions/{name}`：`score` 是**这个人**在这一维的估计（不再是一张卡的分）。
- 理解度：做过测试给一点分（一次，重测不累加）—— 它确实更了解你了（实测新用户 10% → 15%）。

## 六、前端（第二页第二屏 +「轻量测试」屏）

- 能力洞察空态的「开始测试」→ 问卷屏（一次一题、进度 1/22、可回退、末题「完成」）。
- 完成页：「你的起点」+ 每维一条进度条（数值低）+「数值偏低是正常的：它只是坐标系的起点……」
  —— **不出现"自评""不计入综合分"**。
- 回第二页：雷达正常画；说明一行「起点来自那次测试 / X 已经按真实成果动过」，
  **右边不再挂角标**；综合能力那格直接显示数字（≈35–50），不再写"未知"。

## 七、接缝改动（只动了这些）

| 文件 | 改动 | 理由 |
|---|---|---|
| `app/v28/core/screen.ts` | +1 行 | 新增 `questionnaire` 屏的路由契约 |
| `app/v28/core/app-shell.tsx` | +2 处 | 从模块入口导入 `QuestionnairePage`；加一条问卷路由 |
| `app/v28/features/pages24/**`（我们的） | 问卷屏/样式/类型与 API；雷达与说明按新口径 | 这两页归我们 |

## 八、自测（本轮全绿）

`tools/dimension_selftest.py` 20/20 · `questionnaire-check.mjs` 24/24 · `bridge-check` 21/21
（断言按新口径：1 条成果不给维度结论，但样本数要进估计）· `loop-check` 13/13 ·
`page2-sheet-check` ALL GOOD · `sheet-footer` 7/7 · `skill-launch-contract` 9/9 ·
`profile-ability-honors` 两处勋章一致 · `full-sweep-pages24` 21 屏 0 报错 ·
`page2-empty-and-offline` 空态与降级都对 · `page2-field-alignment` ALIGNED ·
`/health/deps` allGreen · 同事 `npm test` 25/25、`test:runtime` 159/159 · 两个仓库 `tsc` 0。

**顺手把后端冒烟测试修绿了**：跑 `smoke_test.py` 的时候它本来是红的（上一轮改口径后没人回来改断言），
而且中途暴露出**两个真 bug**，都修了：

1. `/agent/run` 直接调 `/evidence` 那个**路由函数** → 拆 main.py 时漏了 import（`NameError`），
   补上之后又发现 `Depends`/`Body` 的默认值在直接调用时是**对象不是值**。现在抽出普通函数
   `write_outcome()`，两条链都走它，参数全是显式值。
2. 冒烟里对 `user_skills` 的打桩还是 0 参 lambda（第二页的身份贯通给它加了 `pa_id`），一调就 `TypeError`。

断言也按现行口径改过（不是把数字改成"跑出来多少就是多少"，每条都写了为什么）：
理解度是自己算的 45 分/Lv.2；0 成果的卡**不给分**；人级维度分**不再**是"卡的分加权平均"
（一张 0 成果的卡不会动那一维）；权重表里**本人验收 0.8、不是最高档**；
卡组按**名字**认（这份冒烟自己会产出一份用户的 skill，卡组里本来就不止一条）。
现在 `py -3 smoke_test.py` → `RESULT: ALL PASSED`、`[NG]` 0 条。

截图：`%TEMP%\elfred-poc\questionnaire\`（空态 / 问卷 / 起点 / 雷达 / 有成果后的雷达）。
探针会写数据，跑完自动清（`questionnaire-check.mjs` 最后一步调
`tools/reset_sample_data.py --user <探针账号> --apply`，并断言那个账号回到空态）。
另外新增 `tools/clean_probe_users.py`（只清 `qa*`/`br*`/`lv*`/`lp*`/`py*` 这些**探针账号**，
`demo` 和真人手机号一个都不碰），本轮清掉 52 个探针账号 / 154 行残留。

## 九、没做的（下一步）

- ~~「重新测试」入口~~：**不做**（2026-09-26 明确不要）。问卷只在"能力洞察还是空态"时出现一次；
  后端接口本身是幂等的（重测覆盖并留历史），留着备用但不暴露入口。
- **IPIP 常模**：现在只用了题面与方向键，没有接 `five-factor-e` 的常模做年龄/性别校正 ——
  对"给一个低起点"来说够用，等真有数据再看要不要校准。
- 「陈旧 skill 要不要更新」的问句：后端能算（`/page2/skill-triggers`），界面还没接。
