# Elfred V28 新对话启动提示词

将下面整段复制到新的 Codex 对话中：

```text
你现在接手 Elfred V28 的前端开发。工作目录是：
D:\软工任务\elfred-v28-modular\Elfred-V28

先不要重构，也不要凭旧代码补产品功能。请按以下顺序执行：

1. 完整阅读：
   - D:\软工任务\elfred-v28-modular\Elfred-V28\docs\Elfred-V28-前端开发总文档.md
   - D:\软工任务\Elfred-产品文档规范.md
   - D:\软工任务\Elfred-共用约束.md
   - D:\软工任务\01-首页设计方案.md
   - D:\软工任务\elfred-v28-modular\Elfred-V28\docs\前端模块分工与代码边界.md

2. 检查当前工作树、package.json、app/v28 目录和已有测试。保留其他人的改动，不删除或覆盖无关内容。

3. 启动现有项目，打开 /v28?demo=1，先报告：
   - 项目是否正常运行；
   - 四个主页面是否可达；
   - 控制台、类型检查、测试或构建是否有错误；
   - 当前首页实现与产品文档最明显的差异。

4. 本轮只做前端，不接后端和真实 Agent 框架。Letta 是未来运行时/记忆 POC 候选；Codex 是开发工具；EvoMap、LAAP、Elys 暂不作为当前依赖。

5. 产品状态必须严格区分“已确认、已确认方向、当前实现、设计方案、A/B Test、产品分歧、已废弃”。不得把待讨论项写死。

6. 当前第一项实现任务：在不破坏现有视觉与交互的前提下，审查首页三时段卡代码，为晚间每日总结补齐 Person Agent 反思的前端数据结构和演示交互：
   - 事实层：今日完成、未完成、变化和阻塞；
   - 解释层：偏差原因与替代解释；
   - 理解层：Person Agent 对用户形成的待确认新理解，显示依据和置信度；
   - 行动层：明日延续项，以及是否建议向相关子 Agent 分配新的最小必要上下文；
   - 用户可确认、修正、搁置或否认；
   - 未经确认不得写入稳定画像、广播给五个子 Agent 或自动创建明日任务。

7. 修改范围优先限定在：
   - app/v28/features/home/**
   - 必要时由公共层调整 app/v27-7-state.ts、app/v27-7-data.ts
   - 对应测试
   不要把新逻辑写回 app/v27-7-app.tsx，也不要复制 legacy 形成两套实现。

8. 修改后运行 npm run typecheck、npm test、npm run build，并在浏览器验证 /v28?demo=1。向我汇报：改了什么、为什么、验证结果、仍有哪些产品问题需要拍板。

如果文档与代码冲突，以用户最新拍板 > 产品文档 > 当前 V28 可运行实现 > 历史材料的顺序处理；不要自行扩大需求范围。
```
