# V28 模块化整理变更报告

## 基线

- 唯一产品基线：`Elfred-V28-后端交付完整包(1).zip` 内的 `Elfred-V28-完整项目.zip`。
- 没有把旧运行项目中的产品新增内容并入基线。
- 仅参考并保留了旧运行项目中已验证的工程化整理：跨平台启动、状态/数据拆分、无引用 CSS 清理和无逻辑旧路由清理。

## 本轮完成

1. 新建独立工作目录 `D:/软工任务/elfred-v28-modular/Elfred-V28`。
2. 将原来约 9000 行的主文件改成 4 行兼容入口。
3. 抽出公共导航契约 `app/v28/core/screen.ts`。
4. 抽出全局状态与页面组合层 `app/v28/core/app-shell.tsx`。
5. 将四个主页面拆为四个独立 feature：
   - 首页 `features/home/home-page.tsx`
   - 知识 `features/knowledge/knowledge-page.tsx`
   - 消息 `features/messages/messages-page.tsx`
   - 我的 `features/profile/profile-page.tsx`
6. 尚未按业务域拆开的二级流程集中到 `legacy/legacy-ui.tsx`，维持单一实现和现有行为。
7. 消息页联系人语义修正：真人联系人由 `Agent` 改为 `私聊`，筛选项改为“全部 / 私聊 / 群聊”。
8. 更新测试，使其检查模块化后的真实源码，而不是依赖旧单文件文本。
9. 增加团队分工与代码边界文档。
10. 将生成目录加入 `.gitignore`，避免构建产物进入协作提交。

## 已做的代码减法

- 删除 10 个仅重定向或重复导出的旧版本路由页。
- 删除无引用 CSS（沿用已验证的旧项目清理结果）。
- 不保留旧项目中未经产品确认的新增产品功能。
- 不再在 `v27-7-app.tsx` 维护页面逻辑，减少多人修改同一大文件的冲突。

## 验证结果

- `npm run typecheck`：通过。
- `npm test`：22/22 通过。
- `npm run build`：通过，生成 `/`、`/v27/register`、`/v28`。
- `npm run lint`：0 error，13 个存量 warning，主要位于待继续拆分的 `legacy`。
- 浏览器实测：
  - 首页、知识、消息、我的四个底栏页面均能打开；
  - 消息分类显示“全部 / 私聊 / 群聊”；
  - 浏览器 console 无 warning/error。

## 尚未做完

- `legacy/legacy-ui.tsx` 仍约 7500 行，包含初始化、任务、社区、聊天、设置和多个弹层。
- 下一轮需要按业务域逐组迁出；每迁出一组就删除 legacy 中的对应实现，不能复制形成双份代码。
- 右下角 Elfred 按钮不在本轮范围，继续保留现状等待产品讨论。

## 旧目录处理

本轮没有删除旧运行项目、重复解压目录或原始压缩包。原因是这些目标名称并不唯一，且原始压缩包仍是当前可恢复基线。新目录已经验证通过，下一步可在明确删除清单后清理旧副本。
